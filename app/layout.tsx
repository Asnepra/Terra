import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Video Downloader - Download Videos Easily",
  description:
    "Download videos easily from various sources with our Video Downloader. No ads, no hassle!",
  keywords:
    "video downloader, download videos, video download, free video download",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
